/**
 * A core on a processor with components. It contains a list of components that the core has.
 */

package model;

import java.util.List;

public interface ICore {
	public List<ICoreComponent> _components;
}